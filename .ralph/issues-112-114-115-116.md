Goal: Fix issues #112, #114, #115, and #116 in this worktree and prepare the branch for a PR.

Requirements:
1. Fix issue #112: manual issue verification must not pollute the main run list with durable verification artifacts like cli/cli #1.
2. Fix issue #114: when a new submission matches the same source link as an existing run, reuse the active run and return its run link; if the previous run stopped abnormally, allow a fresh run to be created.
3. Fix issue #115: add a CSV upload batch entrypoint for creating runs from rows that include a run URL and startup context.
4. Fix issue #116: keep run terminal state semantics consistent so preexisting check failures do not make a successful run look like an abnormal exit.
5. Add or update tests for the changed behavior.

Constraints:
1. Work only in this worktree: /home/svtter/work/project/software-factory-ralph-issues
2. Do not modify the main branch worktree.
3. Keep behavior changes scoped to the issues above.
4. Preserve existing patterns unless a direct change is required.
5. If a task is blocked, leave a concrete note in the output and continue with other tasks if possible.

Verification:
1. Run focused tests that cover the changed code paths.
2. If full test execution is not possible, explain exactly what was run and what was missing.
3. Push the branch and open or update a PR against `main`.
4. Final acceptance must include PR-level health, not just local functionality:
   - the PR status must be reviewable
   - the PR should be mergeable, or any merge blocker must be explicitly identified with evidence
   - CI should be passing, or each failing check must be explicitly called out with concrete logs
   - AI review feedback should be positive overall, or every blocking/negative review item must be addressed or explicitly documented
5. Leave the branch in a state ready for review with code changes and tests updated.
6. Treat PR acceptance as first-class work items in tasks mode.
   - Do not stop after code and tests are done.
   - Keep explicit task items for PR inspection, AI review triage, fixes or documented rebuttals, CI re-check, and final mergeability verification.
   - If `ralph-tasks.md` does not contain those acceptance tasks, add them before continuing.
7. Current PR target and known blocker:
   - pull request: https://github.com/sun-praise/software-factory/pull/117
   - current AI review comment: https://github.com/sun-praise/software-factory/pull/117#issuecomment-4107225038
   - do not treat the `AI Code Review` check conclusion by itself as proof that AI review is acceptable
   - inspect the actual review comment content and either fix each material issue or document why it is non-blocking with concrete evidence

Completion promise:
Output exactly <promise>COMPLETE</promise> only when all required issue work is implemented and the final acceptance includes functional verification plus PR readiness: mergeability, CI status, and AI review state. In tasks mode, this also requires the PR acceptance tasks in `ralph-tasks.md` to be marked complete. Otherwise document the remaining blocker with concrete evidence instead of claiming completion.
